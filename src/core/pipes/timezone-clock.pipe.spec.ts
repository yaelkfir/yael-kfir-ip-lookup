import { TimezoneClockPipe } from './timezone-clock.pipe';

describe('TimezoneClockPipe', () => {
  it('create an instance', () => {
    const pipe = new TimezoneClockPipe();
    expect(pipe).toBeTruthy();
  });
});
